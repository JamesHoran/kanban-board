table:
  name: boards
  schema: public
array_relationships:
  - name: columns
    using:
      foreign_key_constraint_on:
        column: board_id
        table:
          name: columns
          schema: public
insert_permissions:
  - role: user
    permission:
      check: {}
      columns:
        - id
        - name
        - owner_id
        - created_at
select_permissions:
  - role: user
    permission:
      columns:
        - id
        - name
        - owner_id
        - created_at
      filter: {}
      allow_aggregations: true
update_permissions:
  - role: user
    permission:
      columns:
        - id
        - name
        - owner_id
        - created_at
      filter: {}
      check: null
delete_permissions:
  - role: user
    permission:
      filter: {}
